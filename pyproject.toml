[project]
name = "trinity-mock-api"
version = "0.1.0"
description = "Mock Trinity Team Server REST API with plausible dummy data (in-memory, dev-only)"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.111",
    "uvicorn>=0.30",
]
